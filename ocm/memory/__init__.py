"""Memory core (W4, W8) and governed stores.

Assertion Builder, Commit Manager, Graph Store (NetworkX), Storage Repository
(SQLite), Quarantine Store, and the Provenance Tracker, plus the pipeline
contract models.
"""
